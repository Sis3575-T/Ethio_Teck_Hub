/**
 * Chat Socket.IO event handler.
 * Full implementation in Task 8.5.
 *
 * @param {import('socket.io').Server} io
 * @param {import('socket.io').Socket} socket
 */
function chatHandler(io, socket) {
  // TODO (Task 8.5): socket.on('join_group', handler)
  // TODO (Task 8.5): socket.on('leave_group', handler)
  // TODO (Task 8.5): socket.on('send_message', handler)
  // TODO (Task 8.5): socket.on('join_room', handler)
  // TODO (Task 8.5): socket.on('send_dm', handler)
}

module.exports = chatHandler;
